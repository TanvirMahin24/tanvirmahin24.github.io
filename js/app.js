/*
const form = document.querySelector('#contact-form');
const sub = document.querySelector('#subject');
const body = document.querySelector('#body');
let url = 'mailto:tanvirmahin24@gmail.com';

let subText ='';
let bodyText = '';



    



form.addEventListener('submit', (e) => {
    subText = encodeURIComponent(sub.value); 
    bodyText = encodeURIComponent(body.value);
    url += (`?subject=${subText}&body=${bodyText}`);
    
    form.action = url;
    
    console.log(form);
    //e.preventDefault();
});

*/